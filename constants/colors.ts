export const primary = "#09342D";
export const secondary = "#77898D";


// TypeScript: Define a type for the colors
export type Colors = {
  primary: string;
  secondary: string;
};

// Optional: Export as an object
export const colors: Colors = {
  primary,
  secondary,
};